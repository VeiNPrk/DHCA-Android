package com.vnprk.dhca.mappers

class MapperUserAPI_toEntity {
    /*companion object{
        fun convert(userApi:UserEntityAPI, token:String) = UserEntity(
            firstName = userApi.firstName,
            lastName = userApi.lastName,
            email = userApi.email,
            token = token,
            lastOnline = userApi.lastOnline,
            lastUpdate = userApi.lastUpdate,
            confirmed = userApi.confirmed
        )
    }*/
}
